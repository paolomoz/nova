/**
 * Asset embedding — images via CLIP/SigLIP.
 * Phase 7: Full implementation with visual embedding pipeline.
 */

export async function embedAsset(
  _assetPath: string,
  _projectId: string,
): Promise<void> {
  // Phase 7: CLIP/SigLIP embedding for visual search
}
