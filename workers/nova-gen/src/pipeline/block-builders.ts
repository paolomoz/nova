// Re-export from shared package — canonical implementation lives in @nova/shared-types
export {
  escapeHTML,
  buildHeroHTML,
  buildCardsHTML,
  buildColumnsHTML,
  buildAccordionHTML,
  buildTabsHTML,
  buildTableHTML,
  buildTestimonialsHTML,
  buildCTAHTML,
  buildBlockHTML,
  buildEDSHTML,
  tryBuildFromJSON,
  type EDSBlock,
} from '@nova/shared-types';
